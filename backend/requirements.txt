# =============================================================================
# SIH26053 ML Backend — Python Dependencies
# =============================================================================
# Core (always needed)
numpy>=1.24
torch>=2.0
pyyaml>=6.0
scipy>=1.10

# Server
fastapi>=0.100
uvicorn[standard]>=0.20
websockets>=11.0
python-multipart>=0.0.6

# Visualization / evaluation helpers
matplotlib>=3.7
scikit-learn>=1.3
tqdm>=4.65

# --- Optional: Cylinder3D ---
# spconv-cu118>=2.3    # install separately: pip install spconv-cu118
# numba>=0.57
# torch-scatter         # install separately matching your torch+cuda

# --- Optional: MMDetection3D / MinkUNet ---
# mmengine>=0.10
# mmcv>=2.0
# mmdet>=3.0
# mmdet3d>=1.4
# MinkowskiEngine>=0.5  # requires custom build with CUDA
